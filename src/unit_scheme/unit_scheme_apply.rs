use crate::page_manager::file_io::FileIO;

use super::UnitScheme;

pub struct UnitSchemeApply {}

impl UnitSchemeApply {
    pub fn apply(file_io: &mut impl FileIO, scheme: &UnitScheme) {
        // file_io.write(, page_i)
        
    }
}
